import { UComponent } from '../../../../lib/UniversalDom';

class LessonResultPage extends UComponent<{}> {
  render(u) {
    return (
      u('div', {className: this.b()})
    );
  }
}

export {
  LessonResultPage
};